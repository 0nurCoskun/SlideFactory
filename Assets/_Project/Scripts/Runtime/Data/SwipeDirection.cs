/// <summary>
/// Kartın fırlatılabileceği yönler.
/// Tüm sistemler (Input, CardData, GameManager) bu ortak enum üzerinden konuşur.
/// </summary>
public enum SwipeDirection
{
    None,
    Up,
    Down,
    Left,
    Right
}
